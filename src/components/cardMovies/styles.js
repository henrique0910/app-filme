import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerFilmes: {
    width: 140,
    marginRight: 15,
    paddingTop: 20,
    paddingBottom: 16,
  },

  images: {
    width: 140,
    height: 180,
    borderRadius: 8,
  },

  titulo: {
    color: '#fff',
    fontSize: 13,
    fontWeight: 'bold',
    paddingTop: 8,
  },

  textNota: {
    color: '#fff',
    fontSize: 11,
    paddingTop: 5,
  },
});

export default styles;
